# docker的安装
```bash
- 更新现有 YUM 包
	# yum update
- 执行 Docker 安装脚本
	# curl -sSL https://get.docker.com/ | sh 
- 启动 Docker 服务	
  # service docker start
  # chkconfig docker on
- 确定 Docker 安装成功
# docker run hello-world

```
